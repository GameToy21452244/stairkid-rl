# Project history

The high-level lineage is V1 → V2 → Fresh V3 → Real/simulator system
identification → corrected flipping physics → V3.5 R1 → R2 → R3 → R4
→ STOP_V3_5 → R4 exploratory Real20.

V2 is retired from the active architecture. The retained historical policies
are Fresh V3 (`STABLE_BASELINE`) and V3.5 R4 (`EXPERIMENTAL_FINAL`). R4 failed
its formal simulator gate, was not promoted, and retains unresolved safety.
Its 20-episode exploratory Real result does not overturn STOP_V3_5.

M4 will complete the detailed history and remove obsolete active workflows;
Git history itself will not be rewritten.
