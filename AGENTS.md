# Codex 工作規範

本專案的後續工作必須遵守：

- 開始前先完整閱讀 `README.md` 與現有測試。
- 不可直接執行未知 `.exe`，除非使用者明確允許。
- 不可注入、修改、掛鉤或反編譯遊戲程序，也不可讀取程序記憶體。
- 不可繞過安全保護或反作弊機制。
- 不可關閉 F8 緊急停止或 PyAutoGUI fail-safe。
- 所有輸入自動化必須具有 `release_all()`，並在例外、失焦及結束時呼叫。
- 自動輸入只能在已確認目標遊戲位於前景時執行。
- 先寫或更新測試，再修改關鍵控制邏輯。
- 每次完成工作後執行 `pytest -q`；自動測試不得操作真實鍵盤或滑鼠。
- 不要把遊戲執行檔、模型、錄影與大量截圖提交到 Git。
- 不要猜測遊戲 UI 位置；必須透過校正工具、已標記樣本或設定檔取得。
- 遊戲路徑、視窗標題與按鍵都必須集中放在設定檔，不可散落於程式碼。
- 所有使用者可見文字優先使用繁體中文。
